## Sentra-GSAP

Treat `/Sentra-GSAP` and `/sentra-gsap` as explicit invocation of the project `sentra-gsap` skill. The authoritative instructions are `.agents/skills/sentra-gsap/SKILL.md`; `.claude/skills/sentra-gsap/SKILL.md` is only a compatibility adapter.

Do not declare Sentra-GSAP work complete until the canonical skill's required verification and independent visual-review gates pass. Never weaken a failing gate simply to obtain PASS.
