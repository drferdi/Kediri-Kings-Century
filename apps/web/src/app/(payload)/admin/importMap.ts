/**
 * Import map admin Payload. Kosong selama belum ada komponen admin kustom.
 *
 * Payload menghasilkan berkas ini lewat `payload generate:importmap` begitu
 * komponen kustom pertama muncul; sampai saat itu peta kosong bertipe lebih
 * jujur daripada berkas .js hasil generate yang tidak pernah dijalankan.
 */
export const importMap: Record<string, unknown> = {};
