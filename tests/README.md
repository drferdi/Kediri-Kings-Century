# Capsule test boundary

Executable tests are owned by `apps/web/tests` (Vitest) and `apps/web/e2e` (Playwright). This
directory satisfies the SAFRS capsule topology without duplicating test code.

Test layers and what each proves: [`../docs/testing.md`](../docs/testing.md).
